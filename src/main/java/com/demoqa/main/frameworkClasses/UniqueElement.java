package com.demoqa.main.frameworkClasses;

import org.openqa.selenium.By;

public class UniqueElement extends BaseElement {
	public UniqueElement(By locator, String name) {
        super(locator, name);
    }
}
